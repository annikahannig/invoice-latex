{
  "number": "{{invoiceNumber}}",
  "date": "{{date}}",
  
  "from": {
    "name": "{{from.name}}",
    "address": [
<%- from.address.split("\n").map(function(line){
return '"'+line+'"';
}).join(",\n"); %>
    ]
  },

  "to": {
    "name": "{{from.name}}",
    "address": [
<%- to.address.split("\n").map(function(line){
  return '"'+line+'"';
}).join(",\n"); %>
    ]
  },

  "items": [
    {
      "type": "Entwicklung",
      "description": "Beschreibung",
      "qty": 2.5,
      "rate": 150.0
    }
  ]
}

