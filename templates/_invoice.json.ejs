{
  "number": "{{invoiceNumber}}",
  "date": "{{date}}",
  
  "from": {
    "name": "{{from.name}}",
    "address": [
      <%= from.address.split("\n").map(function(line){
        return '"'+line+'"';
      }).join(",\n"); %>
    ]
  },

  "to": {
    "name": "{{from.name}}",
    "address": [
      <%= to.address.split("\n").map(function(line){
        return '"'+line+'"';
      }).join(",\n"); %>
    ]
  },

  "items": [
    {
      "type": "Entwicklung",
      "description": "JavaScript-Dateien abgenockt und Zeilenumwälzung verbessert.",
      "qty": 2.5,
      "rate": 85.0
    }
  ]

}

